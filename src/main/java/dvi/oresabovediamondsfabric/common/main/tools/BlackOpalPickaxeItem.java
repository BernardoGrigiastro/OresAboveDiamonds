package dvi.oresabovediamondsfabric.common.main.tools;

import net.minecraft.item.PickaxeItem;
import net.minecraft.item.ToolMaterial;

public class BlackOpalPickaxeItem extends PickaxeItem {
    public BlackOpalPickaxeItem(ToolMaterial material, int attackDamage, float attackSpeed, Settings settings) {
        super(material, attackDamage, attackSpeed, settings);
    }
}
